class Solution:
    def stoneGame(self, piles: List[int]) -> bool:
        # even length array tells us if alice first picks an odd index, all future choices must be odd
        # and if first choice is even, all future choises are even
        return True
